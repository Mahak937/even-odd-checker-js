# Even Odd Checker (JavaScript)

This project contains a simple JavaScript function to check whether a number is even or odd.

## Logic
- Even → number % 2 === 0
- Odd → number % 2 !== 0

## Usage
Run the script using Node.js or in browser console.

## Example
Input: 5  
Output: Odd
